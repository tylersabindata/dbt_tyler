version: 2

models:
  - name: stg_order_info
    description: one unique order per row
    columns:
      - name: order_id
        description: '{{ doc("stg_testing") }}'
        tests:
          - not_null